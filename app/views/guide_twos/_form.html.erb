<%= form_for(guide_two) do |f| %>
  <% if guide_two.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(guide_two.errors.count, "error") %> prohibited this guide_two from being saved:</h2>

      <ul>
      <% guide_two.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
